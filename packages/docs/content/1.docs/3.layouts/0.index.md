# Layouts

:breadcrumbs

TNT uses a barebones `default.vue` layout by default. To configure a different default, you can add a `content/_config.yml` file to your project with the contents:

```yml
layout: article
```

Othe layouts packaged with TNT:

:articles-list
