{
  "name": "tnt",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "lerna run prepare && cd packages/docs && nuxt dev",
    "docs:build": "lerna run prepare && cd packages/docs && nuxt build"
  },
  "devDependencies": {
    "lerna": "^8.1.7"
  }
}
